import styled from 'styled-components';

export const Container = styled.div`
  /*INICIO -  bloco para o container ficar invisível e para a direita do container */
  position: absolute;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden;
  /* FIM */
`;
